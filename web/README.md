# web

React + TypeScript + Vite frontend for the Mispricing Engine draft board.

`npm run dev` starts Vite on :5173 and proxies `/api` to the FastAPI service on :8000.
`npm run build` type-checks and builds. From the repo root, `make dev` runs both halves.

See the root README for what the model does and why it's built this way.
